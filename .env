# Committed default so Vercel (and any fresh clone) builds the demo on mock data
# without extra setup. NEXT_PUBLIC_* is inlined into the client bundle and is not
# a secret. Local dev can override in .env.local (gitignored).
NEXT_PUBLIC_DATA_SOURCE=mock
