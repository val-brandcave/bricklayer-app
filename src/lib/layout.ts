/* Shared shell metrics. One source of truth for the header height so the top
   bar, the nav header cell, the docked copilot header, and the chat thread-rail
   header all land on a single horizontal gridline (they must never drift). */
export const HEADER_H = 60;
